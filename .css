body {
    display: flex;
    justify-content: center; /* Centraliza horizontalmente */
    align-items: center;     /* Centraliza verticalmente */
    min-height: 100vh;      /* Ocupa toda a altura da tela (viewport) */
    margin: 0;              /* Remove margens padrão do body */
    background-color: #f0f0f0; /* Cor de fundo opcional para contrastar */
}

.titulo {
    color: #f2f2f2; 
    text-align: center; 
    padding: 20px; 
    font-size: 15px;
}

header {
    display: flex;
    justify-content: center;
    align-items: center;
}

.content {
    background-color: #333; 
    color: white; 
    padding: 40px; 
    text-align: center; 
    border-radius: 25px;
    width: 80%; 
    font-size: small;
    margin: 2000px auto; /* Margem para espaçamento (opcional) */
}